apiVersion: v1
kind: Service
metadata:
  name: {{ include "trivy-vex-app.fullname" . }}
  labels:
    {{- include "trivy-vex-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trivy-vex-app.selectorLabels" . | nindent 4 }}
