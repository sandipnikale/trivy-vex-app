{{- if .Values.ai.geminiApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: gemini-api-key
  labels:
    {{- include "trivy-vex-app.labels" . | nindent 4 }}
type: Opaque
data:
  GEMINI_API_KEY: {{ .Values.ai.geminiApiKey | b64enc | quote }}
{{- end }}
